export const enum DeviceId {
  def = '{DEVICE_ID}',
}

export const enum DeviceInfo {
  def = '{DEVICE_INFO}',
  new = '{NO_DEVICE_INFO}',
}

export const enum IsInvitedGuestCome {
  Ignored,
  WillBe,
  Pass,
}
