import { CSSProperties, ReactNode } from 'react';

export type FontSizeContainProps = {
  className?: string;
  subUpdates?: string | number;
  style?: CSSProperties;
  content?: ReactNode;
  html?: string;
};
