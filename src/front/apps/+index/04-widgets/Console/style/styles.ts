import styled from '@emotion/styled';

export const StyledIndexConsoleCoderResultName = styled.span`
  & {
    flex-wrap: nowrap;
    color: #bc81a0;
    white-space: nowrap;
    word-wrap: nowrap;
    overflow: auto;
    overflow-wrap: nowrap;
  }
`;
