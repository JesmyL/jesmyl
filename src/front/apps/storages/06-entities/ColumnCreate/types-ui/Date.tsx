import { StoragesColumnType } from 'shared/model/storages/rack.model';
import { StoragesColumnCreateTypeProps } from '../model/model';

export const StoragesColumnCreateOfTypeDate = (props: StoragesColumnCreateTypeProps<StoragesColumnType.Date>) => {
  if (props) return;
  return <></>;
};
