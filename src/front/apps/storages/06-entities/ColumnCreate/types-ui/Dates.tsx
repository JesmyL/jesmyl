import { StoragesColumnType } from 'shared/model/storages/rack.model';
import { StoragesColumnCreateTypeProps } from '../model/model';

export const StoragesColumnCreateOfTypeDates = (props: StoragesColumnCreateTypeProps<StoragesColumnType.Dates>) => {
  if (props) return;
  return <></>;
};
