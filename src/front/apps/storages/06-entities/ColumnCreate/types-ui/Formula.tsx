import { StoragesColumnType } from 'shared/model/storages/rack.model';
import { StoragesColumnCreateTypeProps } from '../model/model';

export const StoragesColumnCreateOfTypeFormula = (props: StoragesColumnCreateTypeProps<StoragesColumnType.Formula>) => {
  if (props) return;
  return <></>;
};
