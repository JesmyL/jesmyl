import { StoragesColumnType } from 'shared/model/storages/rack.model';
import { StoragesColumnCreateTypeProps } from '../model/model';

export const StoragesColumnCreateOfTypeNumber = (props: StoragesColumnCreateTypeProps<StoragesColumnType.Number>) => {
  if (props) return;
  return <></>;
};
